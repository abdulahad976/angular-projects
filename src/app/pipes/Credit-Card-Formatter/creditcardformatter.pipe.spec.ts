import { CreditcardformatterPipe } from './creditcardformatter.pipe';

describe('CreditcardformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
